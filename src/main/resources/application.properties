spring.application.name=Nextage-BackEnd

# Configuração da conexão com o banco de dados MySQL
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/Nextage?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=bubudina
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuração do JPA (Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuração do pool de conexões (opcional, mas recomendado)
spring.datasource.hikari.maximum-pool-size=10

# IGDB API Credentials
igdb.client.id=8ad5rhvegy8l5f6ohxebbdeweud7dq
igdb.client.secret=ml0a395uaz1toxmp15efmg63pg768r

# Sync configuration
igdb.sync.enabled=true
igdb.sync.interval=86400000 # 24 hours in milliseconds
